Arduino project for controlling a VESC motor controller
=======================================================
A simple sketch reading four input buttons and converting to current requests
send to the VESC using its UART protocol.
